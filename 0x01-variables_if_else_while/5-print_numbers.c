#include <stdio.h>

/**
 * main - Prints all single digits of base 10
 * from 0 to 9
 * Return: always 0 (success)
*/

int main(void)
{
	int x;

	for (x = 0; x < 10; x++)

	printf("%d", x);
	printf("\n");

	return (0);
}
